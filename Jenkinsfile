pipeline {
  agent any
  stages {
    stage('print') {
      steps {
        echo 'Start pipeline'
      }
    }
    stage('') {
      steps {
        git(branch: '/master', url: '/t360')
        echo 'START'
      }
    }
  }
}